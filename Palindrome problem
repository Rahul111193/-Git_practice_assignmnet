      
      let str="madam"
      let bag="";
      for (let i=0; i<=str.length-1; i--){
          bag += str[i];
          
      }
       if (bag==str){
           console.log(str,"is palindrome");
       }else {
           console.log(str,"is not a palindrome");
       }